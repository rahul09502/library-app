version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      LIB_APP_SECRET: "change-me"
      LIB_ADMIN_PASS: "admin123"
    volumes:
      - ./instance:/app/instance
    restart: unless-stopped
